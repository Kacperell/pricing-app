import Pricing from "@/components/pricing";

export default function Home() {
  return (
    <main>
      <Pricing />
    </main>
  );
}
//In real production app pricing list would be fetched from a backend here, instead of hardcoded
